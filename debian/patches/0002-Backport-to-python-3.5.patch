From: Kwame Amedodji <kwame-externe.amedodji@edf.fr>
Date: Wed, 26 Jun 2024 10:29:39 +0200
Subject: Support Python 3.5

Forwarded: no
Last-Update: 2023-06-27
---
 setup.py | 39 ++++++++++++++++++++-------------------
 1 file changed, 20 insertions(+), 19 deletions(-)

diff --git a/setup.py b/setup.py
index c2f3077..c6bc2ca 100644
--- a/setup.py
+++ b/setup.py
@@ -17,7 +17,7 @@ except ImportError:
 CYTHON_VERSION_MIN = "0.29.36" # Keep in sync with pyproject.toml
 SLURM_LIB = "libslurm"
 TOPDIR = Path(__file__).parent
-PYTHON_MIN_REQUIRED = (3, 6)
+PYTHON_MIN_REQUIRED = (3, 5)
 
 
 def get_version():
@@ -29,11 +29,11 @@ def get_version():
 
 
 VERSION = get_version()
-SLURM_VERSION = f"{VERSION.major}.{VERSION.minor}"
+SLURM_VERSION = str(VERSION)[:str(VERSION).find('.',3)] #remove after second dot
 
 
 def homepage(*args):
-    url = f"https://pyslurm.github.io/{SLURM_VERSION}"
+    url = "https://pyslurm.github.io/%s" % SLURM_VERSION
     return "/".join([url] + list(args))
 
 
@@ -71,6 +71,7 @@ metadata = dict(
         "Programming Language :: Cython",
         "Programming Language :: Python",
         "Programming Language :: Python :: 3",
+        "Programming Language :: Python :: 3.5",
         "Programming Language :: Python :: 3.6",
         "Programming Language :: Python :: 3.7",
         "Programming Language :: Python :: 3.8",
@@ -89,11 +90,11 @@ metadata = dict(
         "Documentation" : homepage("reference"),
         "Changelog"     : homepage("changelog")
     },
-    python_requires=f">={'.'.join(str(i) for i in PYTHON_MIN_REQUIRED)}",
+    python_requires=">=%s" % '.'.join(str(i) for i in PYTHON_MIN_REQUIRED),
 )
 
 if sys.version_info[:2] < PYTHON_MIN_REQUIRED:
-    raise RuntimeError(f"Python {PYTHON_MIN_REQUIRED} or higher is required.")
+    raise RuntimeError("Python %s or higher is required." % PYTHON_MIN_REQUIRED)
 
 
 class SlurmConfig():
@@ -107,15 +108,15 @@ class SlurmConfig():
     def _find_hdr(self, name):
         hdr = self.inc_full_dir / name
         if not hdr.exists():
-            raise RuntimeError(f"Cannot locate {name} in {self.inc_full_dir}")
+            raise RuntimeError("Cannot locate %s in %s" % (name, self.inc_full_dir))
         return hdr
 
     def _find_lib(self, lib_dir):
-        lib = lib_dir / f"{SLURM_LIB}.so"
+        lib = lib_dir / ("%s.so" % SLURM_LIB)
         if not lib.exists():
-            raise RuntimeError(f"Cannot locate Slurm library in {lib_dir}")
+            raise RuntimeError("Cannot locate Slurm library in %s" % lib_dir)
 
-        print(f"Found {SLURM_LIB} library in {lib}")
+        print("Found %s library in %s" % (SLURM_LIB, lib))
         return lib_dir
 
     @property
@@ -147,14 +148,14 @@ class SlurmConfig():
         vers = int(self._version, 16)
         major = vers >> 16 & 0xFF
         minor = vers >> 8 & 0xFF
-        return f"{major}.{minor}"
+        return "%s.%s" % (major, minor)
 
     def check_version(self):
-        with open(self.slurm_version_h, "r", encoding="latin-1") as f:
+        with open(str(self.slurm_version_h), "r", encoding="latin-1") as f:
             for line in f:
                 if line.find("#define SLURM_VERSION_NUMBER") == 0:
                     self._version = line.split(" ")[2].strip()
-                    print("Detected Slurm version - "f"{self.version}")
+                    print("Detected Slurm version - %s" % self.version)
 
         if not self._version:
             raise RuntimeError("Unable to detect Slurm version")
@@ -162,7 +163,7 @@ class SlurmConfig():
         if Version(self.version) != Version(SLURM_VERSION):
             raise RuntimeError(
                 "Slurm version mismatch: "
-                f"requires Slurm {SLURM_VERSION}, found {self.version}"
+                "requires Slurm %s, found %s" % (SLURM_VERSION, self.version)
             )
 
 
@@ -172,7 +173,7 @@ slurm = SlurmConfig()
 def usage():
     print(
         textwrap.dedent(
-        f"""
+        """
         PySlurm Help
         ------------
             --slurm-lib=PATH    Where to look for the Slurm library (default=/usr/lib64)
@@ -184,8 +185,8 @@ def usage():
                                 You can also instead use the environment
                                 variable SLURM_INCLUDE_DIR.
 
-        Homepage: {homepage()}
-        """
+        Homepage: %s
+        """ % homepage()
         )
     )
 
@@ -203,7 +204,7 @@ def cleanup_build():
         if file.is_file():
             file.unlink()
         else:
-            raise RuntimeError(f"{file} is not a file!")
+            raise RuntimeError("%s is not a file !" % file)
 
 
 def get_extensions():
@@ -252,9 +253,9 @@ def cythongen():
     except ImportError as e:
         msg = "Cython (https://cython.org) is required to build PySlurm."
         raise RuntimeError(msg) from e
-    else:
+    else:    
         if Version(cython_version) < Version(CYTHON_VERSION_MIN):
-            msg = f"Please use Cython version >= {CYTHON_VERSION_MIN}"
+            msg = "Please use Cython version >= %s" % CYTHON_VERSION_MIN
             raise RuntimeError(msg)
 
     cleanup_build()
