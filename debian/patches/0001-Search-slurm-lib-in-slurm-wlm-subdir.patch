From: =?utf-8?q?R=C3=A9mi_Palancher?= <remi-externe.palancher@edf.fr>
Date: Tue, 23 Apr 2019 13:58:45 +0200
Subject: Search slurm lib in slurm-wlm subdir

Debian packages actually install slurm plugins shared libraries into
slurm-wlm subdirectory. This patch makes setup.py properly looks into
this debian specific subdirectory.
---
 setup.py | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/setup.py b/setup.py
index 9a8fb51..be65372 100644
--- a/setup.py
+++ b/setup.py
@@ -106,9 +106,9 @@ def makeExtension(extName):
         extName,
         [extPath],
         include_dirs = ['%s' % SLURM_INC, '.'],
-        library_dirs = ['%s' % SLURM_LIB, '%s/slurm' % SLURM_LIB],
+        library_dirs = ['%s' % SLURM_LIB, '%s/slurm-wlm' % SLURM_LIB],
         libraries = ['slurmdb', 'slurmfull'],
-        runtime_library_dirs = ['%s/' % SLURM_LIB, '%s/slurm' % SLURM_LIB],
+        runtime_library_dirs = ['%s/' % SLURM_LIB, '%s/slurm-wlm' % SLURM_LIB],
         extra_objects = [],
         extra_compile_args = [],
         extra_link_args = [],
